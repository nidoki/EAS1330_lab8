# Linux System User Manual
## Initial Configurations

git config --global user.name “Dorin Kim”

git config --global user.email dhk84@cornell.edu

ssh-keygen --t ed25519

cat ~/.ssh/id_ed25519.

git clone (your key)

ls -alstr

pyco to add text in the file

Type things in the README file and then press ctrl o to update the file locally on your system

git add README.md

To make sure that the updates are loaded into the readme

## Helpful git stuff
git push - Modifies the file on the local computer

git pull
